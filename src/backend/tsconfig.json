{
  "compilerOptions": {
    "target": "ES2016",                       // Specify ECMAScript target version
    "module": "CommonJS",                     // Specify module code generation
    "outDir": "./dist",                       // Redirect output structure to the directory
    "rootDir": "./src",                       // Specify the root directory of input files
    "strict": true,                           // Enable all strict type-checking options
    "esModuleInterop": true,                  // Enables emit interoperability between CommonJS and ES Modules
    "skipLibCheck": true,                     // Skip type checking of declaration files
    "forceConsistentCasingInFileNames": true, // Disallow inconsistently-cased references to the same file
    "resolveJsonModule": true,                // Include modules imported with .json extension
    "sourceMap": true,                        // Generate corresponding '.map' file
    "baseUrl": ".",                           // Base directory to resolve non-absolute module names
    "paths": {                                // Module path mapping
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],                     // Specifies the files to be included in compilation
  "exclude": ["node_modules", "dist", "**/*.test.ts"] // Specifies the files to be excluded
} 